void qsort(int low, int high) {
    void swap(int i, int j) {
        int x;
        x = a[i];
        a[i] = a[j];
        a[j] = x;
    }
    int parti(int lowhigh) {
        void swap(int i, int j) {
            int x;
            x = a[i];
            a[i] = a[j];
            a[j] = x;
        }
        int i, j, k, key;
    }
}
